import { Component } from '@angular/core';

/**
 * Placeholder for the users section. It exists so the sidebar entry has
 * somewhere to navigate to; the actual user management lands next.
 */
@Component({
  selector: 'app-user-list',
  templateUrl: './user-list.html',
})
export class UserList {}

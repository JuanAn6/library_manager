// Base URL of the Spring Boot API. Kept in one place so the auth service and
// the interceptor cannot drift apart about what "our own backend" means.
export const API_BASE_URL = 'http://localhost:8080/api';
